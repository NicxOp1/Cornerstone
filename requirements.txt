# Pins alineadas al venv real (pip freeze 2026-07-06).
# Las pins anteriores (fastapi 0.95 / pydantic 1.10) eran de la era pre-refactor
# y hubieran roto el deploy: el codigo usa API de Pydantic v2 (ConfigDict, model_dump).
setuptools>=42
wheel
fastapi==0.116.1
uvicorn==0.35.0
requests==2.32.3
httpx==0.28.1
python-dotenv==1.0.1
pytz==2025.2
pydantic==2.13.4
retell-sdk==5.53.0
gspread==6.1.2
google-auth==2.32.0
